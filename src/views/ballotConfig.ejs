<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Ballot</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>

    <div class="container mt-5">
        <h1>Edit Ballot</h1>

        <form id="ballotForm" method="post"
            action="/admin/editBallot?electionTitle=<%=electionTitle%>&startDate=<%=startDate%>&endDate=<%=endDate%>&electionId=<%=electionId%>">
            <div id="positionsContainer">
                <div class="form-group">
                    <h3>Election Title: <%= electionTitle %>
                    </h3>
                    <h3>Start Date: <%= startDate %>
                    </h3>
                    <h3>End Date: <%= endDate %>
                    </h3>
                </div>
                <div class="form-group">
                    <label for="position1">Position Title:</label>
                    <input type="text" class="form-control position-title" name="position" required>
                    <label for="position1Candidates">Candidates (new line per candidate):</label>
                    <div id="position1Candidates">
                            <textarea  class="form-control mb-2" name="candidates" placeholder="example:
        John
        Michael
        Luke" rows="5" required></textarea>
                    </div>
                </div>
            </div>
            <button type="submit" class="btn btn-success mt-3">Save Changes</button>
        </form>
    </div>

</body>

</html>