<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Ballot</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>

    <div class="container mt-5 w-50">
        <h1 class="h1 text-center text-primary">Create Ballot</h1>

        <form id="ballotForm" method="post" class="my-5"
            action="/admin/editBallot?electionTitle=<%=electionTitle%>&startDate=<%=startDate%>&endDate=<%=endDate%>&electionId=<%=electionId%>">
            <div id="positionsContainer">
                <div class="form-group">
                    <h3 class="h4 text-center">Election Title: <%= electionTitle %>
                    </h3>
                </div>
                <div class="form-group">
                    <label for="position1" class="mt-5">Position Title:</label>
                    <input type="text" class="form-control position-title mb-3" name="position" required>
                    <label for="position1Candidates">Candidates (new line per candidate):</label>
                    <div id="position1Candidates">
                            <textarea  class="form-control mb-2" name="candidates" placeholder="example:
        John
        Michael
        Luke" rows="5" required></textarea>
                    </div>
                </div>
            </div>
            <div class="container text-end d-flex justify-content-end">
                <a class="btn btn-danger mt-3 mx-2" href="/admin/cancelEditBallot?electionTitle=<%=electionTitle%>&startDate=<%=startDate%>&endDate=<%=endDate%>&electionId=<%=electionId%>">Cancel</a>
                <button type="submit" class="btn btn-success mt-3">Save Changes</button>
            </div>
            
        </form>
    </div>

</body>

</html>